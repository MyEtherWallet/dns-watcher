{
  "name": "dnsWatcher",
  "version": "0.0.1",
  "scripts": {
    "prestart" : "cd ./MewChecker && npm run build",
    "start": "node server.js",
    "postinstall": "cd ./MewChecker && npm install",
    "build": "cd ./MewChecker && npm run build"
  },
  "dependencies": {
    "cli-progress": "^1.8.0",
    "clone": "^1.0.4",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "ip": "^1.1.5",
    "request": "^2.85.0",
    "request-promise-native": "^1.0.5"
  }
}
